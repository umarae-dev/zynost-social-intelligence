# Copy to .env (gitignored). Never commit .env.
# Empty values mean the adapter is not_configured (no network, no invented data).
# Do not auto-load this file; export names into the process environment (CFG-04).
X_API_KEY=
X_BEARER_TOKEN=
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=
TELEGRAM_API_ID=
TELEGRAM_API_HASH=
DISCORD_BOT_TOKEN=
REDIS_URL=
